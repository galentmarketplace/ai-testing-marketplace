apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ai-testing
resources:
  - 00-namespace.yaml
  - 10-rbac.yaml
  - 30-platform.yaml
  - 40-ingress.yaml
  - 50-sample-app.yaml
# 20-secret.example.yaml is intentionally NOT included — create the real secret from your .env:
#   kubectl -n ai-testing create secret generic marketplace-env --from-env-file=.env
